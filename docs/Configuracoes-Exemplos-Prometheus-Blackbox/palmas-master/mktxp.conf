[Router GrupoWink Esc_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.70.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router GrupoWink ASAG_PON]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.71.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router GrupoWink AGAPE_PON]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.74.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router GrupoWink Cedro_SRT]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.75.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[AccessPoint_PTP GrupoWink Torre Cedro_SRT]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.75.41    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = True    # WLAN general metrics
    wireless_clients = True    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[AccessPoint_PTP GrupoWink 2Torre Cedro_SRT]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.75.42    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = True    # WLAN general metrics
    wireless_clients = True    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router MAC_Hotel MAC_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.45.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router ServCont Esc_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = cli-sc-pmw-fw.ddns.skillsit.com.br    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router GSM Transportes GS_BAL]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.53.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = gsm    # RouterOS user, needs to have 'read' and 'api' permissions
    password = 12345678
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router WGA Contabil WGA_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = cli-wg-pmw-fw.ddns.skillsit.com.br    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router MAV Transportes MAV_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = cli-mav-pmw-fw.ddns.skillsit.com.br    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router Skills CasaFante SK_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = casa-fante.ddns.skillsit.com.br    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router Aprosoja AP_PMW]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = cli-aps-pmw-fw.ddns.skillsit.com.br    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[Router GSM Transportes GS_SLZ_PostoValen]
    enabled = True    # turns metrics collection for this RouterOS device on / off
    hostname = 172.16.54.1    # RouterOS IP address
    port = 8728    # RouterOS API / API-SSL service port
    username = skills    # RouterOS user, needs to have 'read' and 'api' permissions
    password = Sk@2023,Mk
    use_ssl = False    # enables connection via API-SSL servis
    no_ssl_certificate = False    # enables API_SSL connect without router SSL certificate
    ssl_certificate_verify = False    # turns SSL certificate verification on / off   
    installed_packages = True    # Installed packages
    dhcp = True    # DHCP general metrics
    dhcp_lease = True    # DHCP lease metrics
    connections = True    # IP connections metrics
    connection_stats = True    # Open IP connections metrics 
    pool = True    # Pool metrics
    interface = True    # Interfaces traffic metrics
    firewall = True    # IPv4 Firewall rules traffic metrics
    ipv6_firewall = False    # IPv6 Firewall rules traffic metrics
    ipv6_neighbor = False    # Reachable IPv6 Neighbors
    poe = False    # POE metrics
    monitor = True    # Interface monitor metrics
    netwatch = False    # Netwatch metrics
    public_ip = True    # Public IP metrics
    route = True    # Routes metrics
    wireless = False    # WLAN general metrics
    wireless_clients = False    # WLAN clients metrics
    capsman = False    # CAPsMAN general metrics
    capsman_clients = False    # CAPsMAN clients metrics    
    user = False    # Active Users metrics
    queue = False    # Queues metrics
    remote_dhcp_entry = None    # An MKTXP entry for remote DHCP info resolution (capsman/wireless)
    use_comments_over_names = True    # when available, forces using comments over the interfaces names
    check_for_updates = True    # check for available ROS updates

[default]
    monitor = True
    route = True
    user = True
    enabled = False
    no_ssl_certificate = False
    ipsec = False
    dns = False
    wireless_clients = True
    interface = True
    connection_stats = False
    kid_control_dynamic = False
    wireless = True
    ssl_certificate_verify = False
    queue = True
    dhcp = True
    ipv6_pool = False
    ipv6_neighbor = False
    pool = True
    neighbor = True
    capsman_clients = True
    bgp = False
    routing_stats = False
    lte = False
    certificate = False
    ipv6_route = False
    capsman = True
    check_for_updates = False
    plaintext_login = True
    kid_control_assigned = False
    public_ip = True
    ipip = False
    switch_port = False
    use_ssl = False
    installed_packages = True
    use_comments_over_names = True
    dhcp_lease = True
    ipv6_firewall = False
    poe = True
    eoip = False
    gre = False
    netwatch = True
    connections = True
    firewall = True
    hostname = localhost
    username = user
    password = password
    remote_dhcp_entry = None
    remote_capsman_entry = None
    port = 8728
